import React from 'react';

const TestSection = ({ bgColor }) => {
  return (
    <div style={{ background: bgColor }} className="test-section">
      Woof
    </div>
  );
};

export default TestSection;
